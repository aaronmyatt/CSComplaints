<div id="<%= id %>" class="modal is-small has-text-centered">
  <div class="modal-background"></div>
  <div class="modal-card">
    <div class="modal-card-body">
      
      <%= render partial: 'shared/facebook', locals: {text: 'Login with Facebook'} %>

      <%= render partial: 'shared/or' %>

      <%= render partial: 'shared/register_form' %>

    </div>
  </div>
  <button class="modal-close"></button>
</div>
