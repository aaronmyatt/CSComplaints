<section class="hero is-medium is-primary">

  <%= render partial: 'layouts/nav' %>
  
  <div class="hero-body has-text-centered">
    <h1 class="title">Crowd-sourced complaints</h1>
    <h2 class="subtitle">Subtitle</h2>

    <a class="button is-white-outline is-outlined">How it works</a>
    <a class="button is-primary is-inverted">Submit Issue</a>
  </div>

</section>

<section class="section">

  <div class="container">
      <%= render partial: 'complaint_item', collection: @complaints, as: :complaint %>
  </div>

</section>









<!-- <p id="notice"><%= notice %></p>

<h1>Listing Complaints</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @complaints.each do |complaint| %>
      <tr>
        <td><%= link_to 'Show', complaint %></td>
        <td><%= link_to 'Edit', edit_complaint_path(complaint) %></td>
        <td><%= link_to 'Destroy', complaint, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Complaint', new_complaint_path %> -->
